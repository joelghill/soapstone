# Node.js Environment
NODE_ENV=development
LOG_LEVEL=debug
HOST=localhost
PORT=3000
PUBLIC_URL=http://localhost:3000

# PostgreSQL Database Configuration
# For Docker: use 'postgres' as DB_HOST
# For local: use 'localhost' as DB_HOST
DB_URL=postgres://postgres:your_password_here@localhost:5432/soapstone
DB_HOST=localhost
DB_PORT=5432
DB_NAME=soapstone
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_CA_CERT= # Optional: SSL CA certificate in PEM format if needed
