# Node.js Environment
NODE_ENV=development
HOST=localhost
PORT=3000
PUBLIC_URL=http://localhost:3000

# PostgreSQL Database Configuration
# For Docker: use 'postgres' as DB_HOST
# For local: use 'localhost' as DB_HOST
DB_HOST=localhost
DB_PORT=5432
DB_NAME=soapstone
DB_USER=postgres
DB_PASSWORD=your_password_here

# For production, you might want to enable SSL
# DB_SSL=true

# Session Cookie Secret (generate a secure random string for production)
COOKIE_SECRET=00000000000000000000000000000000

# Docker-specific settings (when using docker-compose)
# Uncomment and modify these when running with Docker:
# DB_HOST=postgres
# DB_PASSWORD=postgres

# Production settings
# COOKIE_SECRET=your-super-secure-random-string-here
# PUBLIC_URL=https://your-domain.com
# DB_SSL=true
